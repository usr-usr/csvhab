<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <!-- 2. 収支ボックス-->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/boxBalance"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- 収支ラベル -->
        <TextView
            android:id="@+id/labelBalance"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="過去31日の収支"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 収入ラベル -->
        <TextView
            android:id="@+id/labelIncome"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="収入"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/labelBalance" />

        <!-- 収入金額 -->
        <TextView
            android:id="@+id/textIncome"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textSize="20sp"
            android:textColor="@color/income"
            app:layout_constraintEnd_toEndOf="@id/labelIncome"
            app:layout_constraintTop_toBottomOf="@id/labelIncome" />

        <!-- 支出ラベル -->
        <TextView
            android:id="@+id/labelExpense"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="支出"
            app:layout_constraintEnd_toEndOf="@id/labelIncome"
            app:layout_constraintTop_toBottomOf="@id/textIncome" />

        <!-- 支出金額 -->
        <TextView
            android:id="@+id/textExpense"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textSize="20sp"
            android:textColor="@color/expense"
            app:layout_constraintEnd_toEndOf="@id/labelExpense"
            app:layout_constraintTop_toBottomOf="@id/labelExpense" />

        <!-- 残高 -->
        <TextView
            android:id="@+id/textBalance"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="0"
            android:textSize="36sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/labelBalance" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- 過去31日の履歴ラベル -->
    <TextView
        android:id="@+id/labelRecentRecords"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="過去31日の履歴"
        android:textSize="18sp"
        android:textStyle="bold"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:paddingTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/boxBalance"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 履歴リスト -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:contentDescription="過去31日の取引リスト"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        app:layout_constraintTop_toBottomOf="@+id/labelRecentRecords"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:listitem="@layout/activity_record_scrollwindow"
        tools:itemCount="5"/>

</androidx.constraintlayout.widget.ConstraintLayout>